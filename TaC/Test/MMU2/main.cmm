public void main() {
	out(0xa2, 0x0001);   // MMU 有効化

  // フレーム初期化(NO命令で埋める)
  int[] Mem = _ItoA(0x8000);
  for (int i=0; i<0x0100; i=i+1) {
     Mem[i] = 0x0000;
  }

	Mem[0] = ;

	out(0xa6, );   //ページテーブルレジスタへの出力

  // ユーザモードで実行開始
  setJmp(0x0000, 0x0080);
}